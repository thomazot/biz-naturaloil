.add-to-links
    .link-wishlist
        a
            display: flex
            align-items: center
            padding: 16px 0
            color: var(--text)
            font-size: 12px

        .ico
            flex: 0 0 24px
            margin-right: 5px
            width: 24px
            height: 28px
            fill: var(--secundary)